'use client';
import { useState } from 'react'; import { useRouter } from 'next/navigation'; import Link from 'next/link';
export default function Login(){const router=useRouter();const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const [loading,setLoading]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setError('');setLoading(true);try{const r=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password}),cache:'no-store'});const d=await r.json();if(!r.ok){if(d.requiresVerification){router.replace(`/verify-email?email=${encodeURIComponent(d.email||email)}`);return}throw new Error(d.error)}router.replace('/dashboard')}catch(err){setError(err instanceof Error?err.message:'Login failed.')}finally{setLoading(false)}}return <main className="flex min-h-screen items-center justify-center px-5"><form autoComplete="on" onSubmit={submit} className="w-full max-w-md rounded-3xl border border-zinc-800 bg-zinc-950 p-7"><Link href="/" className="text-sm text-zinc-500">Yassin AI Study</Link><h1 className="mt-8 text-3xl font-semibold">Welcome back</h1><p className="mt-2 text-zinc-500">Your device will stay signed in for 30 days.</p><div className="mt-7 space-y-4"><input required autoComplete="username" type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email" className="w-full rounded-xl border border-zinc-800 bg-zinc-900 px-4 py-3 outline-none focus:border-cyan-300"/><input required autoComplete="current-password" type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password" className="w-full rounded-xl border border-zinc-800 bg-zinc-900 px-4 py-3 outline-none focus:border-cyan-300"/>{error&&<p className="text-sm text-red-300">{error}</p>}<button type="submit" disabled={loading} className="w-full rounded-xl bg-cyan-300 py-3 font-semibold text-black disabled:opacity-50">{loading?'Signing in…':'Sign in'}</button></div><p className="mt-6 text-center text-sm text-zinc-500">No account? <Link className="text-cyan-300" href="/signup">Create one</Link></p></form></main>}
