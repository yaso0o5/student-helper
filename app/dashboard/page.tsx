import { redirect } from 'next/navigation';
import { getCurrentUser } from '@/lib/auth';
export default async function Dashboard(){const user=await getCurrentUser();if(!user)redirect('/login');return <main className="min-h-screen px-5 py-8 md:px-10"><div className="mx-auto max-w-6xl"><header className="flex items-center justify-between border-b border-zinc-800 pb-6"><div><p className="text-sm text-cyan-300">Student Helper</p><h1 className="mt-2 text-3xl font-semibold">Good to see you, {user.name.split(' ')[0]}.</h1></div><form action="/api/auth/logout" method="post"><button className="rounded-xl border border-zinc-700 px-4 py-2 text-sm text-zinc-300">Log out</button></form></header><section className="mt-10 grid gap-4 md:grid-cols-3"><div className="rounded-2xl border border-zinc-800 bg-zinc-950 p-6"><p className="text-sm text-zinc-500">Study sessions</p><p className="mt-3 text-3xl font-semibold">0</p></div><div className="rounded-2xl border border-zinc-800 bg-zinc-950 p-6"><p className="text-sm text-zinc-500">Quiz average</p><p className="mt-3 text-3xl font-semibold">—</p></div><div className="rounded-2xl border border-zinc-800 bg-zinc-950 p-6"><p className="text-sm text-zinc-500">Current streak</p><p className="mt-3 text-3xl font-semibold">0 days</p></div></section><section className="mt-8 rounded-3xl border border-zinc-800 bg-zinc-950 p-8"><p className="text-sm uppercase tracking-widest text-cyan-300">Ready to start?</p><h2 className="mt-3 text-3xl font-semibold">Create your first study session.</h2><p className="mt-3 max-w-xl text-zinc-500">Choose a subject and topic, then use AI to turn it into focused notes and practice.</p><a href="/dashboard/new" className="mt-6 inline-block rounded-xl bg-cyan-300 px-5 py-3 font-semibold text-black">New study session</a></section></div></main>}
