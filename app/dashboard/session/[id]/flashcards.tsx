'use client';
import { useState } from 'react';
export default function Flashcards({items}:{items:string[]}){const [index,setIndex]=useState(0);const [flipped,setFlipped]=useState(false);if(!items.length)return null;const text=items[index];return <section className="rounded-3xl border border-zinc-800 bg-zinc-950 p-7"><div className="flex items-center justify-between"><div><p className="text-sm uppercase tracking-widest text-cyan-300">Quick review</p><h2 className="mt-2 text-2xl font-semibold">AI Flashcards</h2></div><span className="text-sm text-zinc-500">{index+1}/{items.length}</span></div><button type="button" onClick={()=>setFlipped(!flipped)} className="mt-6 min-h-48 w-full rounded-3xl border border-cyan-500/20 bg-gradient-to-br from-cyan-500/10 to-zinc-900 p-8 text-left transition hover:border-cyan-300/50">{flipped?<><p className="text-xs uppercase tracking-widest text-cyan-300">Answer / concept</p><p className="mt-4 text-xl leading-8 text-zinc-100">{text}</p></>:<><p className="text-xs uppercase tracking-widest text-zinc-500">Think about it</p><p className="mt-4 text-xl font-semibold">What is the key idea here?</p><p className="mt-3 text-sm text-zinc-500">Tap to reveal</p></>}</button><div className="mt-5 flex gap-3"><button disabled={index===0} onClick={()=>{setIndex(index-1);setFlipped(false)}} className="flex-1 rounded-xl border border-zinc-800 py-3 disabled:opacity-30">Previous</button><button onClick={()=>{setIndex((index+1)%items.length);setFlipped(false)}} className="flex-1 rounded-xl bg-cyan-300 py-3 font-semibold text-black">Next</button></div></section>}
